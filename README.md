# marrowbuster: a homepage

Homepage made with Vue.js 3 and Vite CLI.

## Work
- project setup: `npm install`
- compile and hot-reload for development: `npm run dev`
- compile and minify for production: `npm run build`
